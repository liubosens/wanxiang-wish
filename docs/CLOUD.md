# 云开发集合

在微信开发者工具云开发控制台创建：

| 集合 | 说明 |
|---|---|
| `users` | 钱包、背包、图鉴、保底状态 |
| `history` | 抽卡流水（建议对 `openid + at` 建索引） |
| `draw_tokens` | 防重放 token（可设 TTL） |
| `pools`（可选） | 若改为远程配置，可将 JSON 迁入此表 |

权限：用户仅读写自己的 `openid` 数据；`draw` 云函数使用管理端权限写库。
